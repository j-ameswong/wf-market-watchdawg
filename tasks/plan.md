# Plan

No capability is in progress. The next deliverable is seconds-level latency (SPEC §3.4, step 3):
C5's socket feeds the same ingest path and the same underpriced rule, and must send `crossplay`
explicitly (ADR-0002). Plan it here, with its acceptance checks in `tasks/todo.md`, before any
code is written. The 72h alert-budget run (C9a acceptance) is still to do.
